<Page x:Class="PL.ParcelInCustomerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PL"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ParcelInCustomerPage">

    <ListView x:Name="ParcelsListView" Background="Gray" Foreground="#2F3E46" FontFamily="Agency FB" FontSize="15" FontWeight="Normal" HorizontalAlignment="Left" VerticalAlignment="Top" BorderBrush="#89141414" >
        <ListView.View>
            <GridView AllowsColumnReorder="True" ColumnHeaderToolTip="information">
                <GridViewColumn DisplayMemberBinding="{Binding Path=Id}" Header="ID" Width="80"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=Weight}" Header="Weight" Width="90"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=Priority}" Header="Priority" Width="95"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=Status}" Header="Status" Width="135"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=CustomerInDelivery}" Header="Customer" Width="140"/>
            </GridView>
        </ListView.View>
    </ListView>
</Page>
